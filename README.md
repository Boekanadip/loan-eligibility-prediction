# Loan Eligibility Prediction\n\nThis repository contains a cleaned and restructured notebook for a loan eligibility prediction project by Adib Raihan Ashidiq (Boekanadip). The cleaned notebook lives on branch `clean/final_task_notebook`.\n\n## What I changed\n- Cleaned and restructured `final_task.ipynb` to improve readability and reproducibility.\n- Added `requirements.txt` listing main Python dependencies.\n- Notebook uses a RandomForestClassifier with `random_state=42`.\n\n## How to run\n1. Place the dataset `loan_data_2007_2014.csv` in a folder named `data/` at the repo root.\n2. Create a Python environment and install dependencies:\n\n```bash\npython -m venv .venv\nsource .venv/bin/activate  # or .\\venv\\Scripts\\activate on Windows\npip install -r requirements.txt\n```\n\n3. Open `final_task.ipynb` with Jupyter/Colab and run cells.\n\n## Notes\n- For portfolio/demonstration speed the notebook may sample the dataset before training; change sampling or remove it to train on full data.\n- Consider adding `environment.yml` or pinned versions for full reproducibility.\n